import React from 'react';
import VideoList from './VideoList';

const Home = () => {
    return (
        <div>
            This is the home component
            <VideoList />
        </div>
    );
}

export default Home;
